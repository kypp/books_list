<h1>Listing books</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th>Title</th>
      <th></th>
      <th>Author</th>
      <th></th>
      <th>Rating</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><img class="cover_image" src="<%= book.cover_image_uri %>" /></td>
        <td></td>
        <td><%= book.title %></td>
        <td></td>
        <td><%= book.author %></td>
        <td></td>
        <td><%= book.rating %></td>
        <td></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
